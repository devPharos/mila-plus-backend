const { fileURLToPath } = require('url')

const { resolve, dirname } = require('path')

const filename = fileURLToPath(import.meta.url)
const directory = dirname(filename)

module.exports = {
    config: resolve(directory, 'src', 'config', 'database.js'),
    'models-path': resolve(directory, 'src', 'app', 'models'),
    'migrations-path': resolve(directory, 'src', 'database', 'migrations'),
    'seeders-path': resolve(directory, 'src', 'database', 'seeders'),
}
